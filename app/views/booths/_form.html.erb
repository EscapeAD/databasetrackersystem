<%= form_for([event, booth]) do |f| %>
  <% if booth.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(booth.errors.count, "error") %> prohibited this booth from being saved:</h2>

      <ul>
      <% booth.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :start %>
    <%= f.date_select :start %>
  </div>

  <div class="field">
    <%= f.label :end %>
    <%= f.date_select :end %>
  </div>

  <div class="field">
    <%= f.label :sponser %>
    <%= f.text_field :sponser %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
